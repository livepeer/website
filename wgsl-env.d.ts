// Types every `.wgsl` import as the ShaderSource the loader emits. One line,
// referenced rather than declared, so it stays right if the shape changes.
/// <reference types="@vgpu/wgsl/wgsl-types" />
